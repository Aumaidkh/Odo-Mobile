*.iml
.kotlin
.gradle
**/build/
xcuserdata
!src/**/build/
local.properties
.idea
.DS_Store
captures
.externalNativeBuild
.cxx
*.xcodeproj/*
!*.xcodeproj/project.pbxproj
!*.xcodeproj/xcshareddata/
!*.xcodeproj/project.xcworkspace/
!*.xcworkspace/contents.xcworkspacedata
**/xcshareddata/WorkspaceSettings.xcsettings
node_modules/
/issues/

# Supabase setup notes — kept local. The bootstrap script names a specific project
# and the module notes go with it; docs/DB_SCHEMA.md stays the versioned spec.
docs/SUPABASE_BOOTSTRAP.md
infrastructure/supabase/README.md

# Firebase project config — identifies a specific Firebase project, dropped in
# locally by whoever is testing against it rather than committed.
androidApp/google-services.json

# Working notes for in-progress slices — kept local, not part of the versioned docs set.
docs/TRIPTRACKER_PLAN.md
docs/AUTO_ODOMETER_PLAN.md

# Kotlin's generated SwiftPM linkage for iOS native dependencies (Firebase's SDK).
# Regenerated on every build from the Gradle dependency graph; not a hand-authored
# lockfile worth reviewing in a diff.
.swiftpm-locks/
